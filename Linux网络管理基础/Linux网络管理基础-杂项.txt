1. 查看默认网关的方法是__________________________；
2. 查看默认网关的方法是__________________________；
3. 查看默认网关的方法是__________________________；
4. hostnamectl set-hostname gzy直接就能永久修改主机名了嘛？_____________________；
5. hostnamectl set-hostname gzy直接就能永久修改主机名了嘛？_____________________；
6. hostnamectl set-hostname gzy直接就能永久修改主机名了嘛？_____________________；


1. ip -4 route show
2. ip -4 route show
3. ip -4 route show
4. 是的，既会更新当前主机名，也会更新/etc/hostname文件
5. 是的，既会更新当前主机名，也会更新/etc/hostname文件
6. 是的，既会更新当前主机名，也会更新/etc/hostname文件